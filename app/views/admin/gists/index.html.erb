<h1>Gists</h1>

<table border = 1>
  <% @gists.each do |gist| %>
  <tr>
    <td><%= gist.user.email %></td>
    <td><%= link_to truncate(gist.question.body, length: 25), question_path(gist.question) %></td>
    <td><%= link_to 'gist', gist.gist, target: :blank %></td>
  </tr>
  <% end %>
</table>
